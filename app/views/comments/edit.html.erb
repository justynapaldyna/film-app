Edit comment:
<%= form_for([@movie, @comment], method: :put) do |f| %>
    <div class="field">
        <%= f.text_area :body %>
    </div>
    <div class="actions">
        <%= f.submit  %>
    </div>
<% end %>