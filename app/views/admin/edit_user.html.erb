<%= form_with(model: @user, url: admin_update_user_path(@user), method: "patch") do |form| %>
    
    <div>
        <%= form.label :status %>
        <%= form.select :status, User.statuses.keys.map{ |key| [key.humanize, key] }, selected: @user.status || :draft %>
    </div>

    <div>
        <%= form.submit %>
    </div>

<% end %>

